# Ejemplo de archivo .env para configurar los scripts de deploy

# ==== CONFIGURACIÓN PRINCIPAL ====
# Estas variables son OBLIGATORIAS para el funcionamiento de las herramientas

# Host SSH (debe estar configurado en ~/.ssh/config)
REMOTE_SSH="ttContaboUsWest"
# También se puede usar REMOTE_SSH_ALIAS que es reconocido por compatibilidad
# REMOTE_SSH_ALIAS="ttContaboUsWest"

# Ruta en el servidor remoto
REMOTE_PATH="/home/runcloud/webapps/tiendattamayocom/"

# Ruta local del proyecto WordPress
LOCAL_PATH="/ruta/a/tu/proyecto/wordpress/app/public/"

# ==== CONFIGURACIÓN DE BASE DE DATOS ====
# Base de datos remota
REMOTE_DB_NAME="nombre_db_remota"
REMOTE_DB_USER="usuario_db_remota"
REMOTE_DB_PASS="contraseña_db_remota"
REMOTE_DB_HOST="localhost"

# URLs para sustitución
REMOTE_URL="https://www.example.com"
LOCAL_URL="https://example.ddev.site"

# ==== CONFIGURACIÓN DE MEDIOS ====
# URL para archivos multimedia
WP_MEDIA_URL="https://cdn.example.com/wp-content/uploads/"
# Usar modo experto (opcional, 0 o 1)
WP_MEDIA_EXPERT=0
# Ruta física (opcional, solo para modo experto)
WP_MEDIA_PATH="../media"
# Ruta a WordPress dentro de DDEV
WP_PATH="app/public"

# ==== CONFIGURACIÓN DE SEGURIDAD ====
# Flag de seguridad para despliegues a producción (enabled/disabled)
# IMPORTANTE: Establece en 'enabled' para prevenir despliegues accidentales
PRODUCTION_SAFETY="enabled" 